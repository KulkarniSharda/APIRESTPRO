package com.gorest.tests;

public class EndToEndTesto {

/*	@Test
	public void createSingleUser() {

		UserLombok user = new UserLombok("Ishika", "Female", StringUtils.randomEmailID(), "active");

		RestClient rest = new RestClient();
		int id = rest.doPost("/public/v2/users", "Json", user, true).then().log().all().assertThat().statusCode(201)
				.extract().path("id");

		System.out.println("Id ==>" + id);

		RestClient restGet = new RestClient();
		rest.doGET("/public/v2/users/" + id, true).then().log().all().statusCode(200);

	}

*/
}